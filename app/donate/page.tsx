import type { Metadata } from "next";
import { GiveHopeContent } from "@/app/give-hope/page";

export const metadata: Metadata = {
  title: "Donate",
  description: "Donate to help children and families continue their journey with care, dignity, and hope.",
  alternates: { canonical: "/donate" },
};

export default GiveHopeContent;
